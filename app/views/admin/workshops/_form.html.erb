<%= form_for [:admin, @workshop] do |f| %>

  <article class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </article>

  <div class="row">
    <div class="col-md-6">
      <article class="form-group">
        <%= f.label :start_date %>
        <%= f.date_field :start_date, class: 'form-control' %>
      </article>
    </div>

    <div class="col-md-6">
      <article class="form-group">
        <%= f.label :start_time %>
        <%= f.time_field :start_time, class: 'form-control' %>
      </article>
    </div>
  </div>

  <article class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: 'form-control' %>
  </article>

  <article class="form-group">
    <%= f.label :state %>
    <%= f.select :state, options_for_select(Workshop.states.map {|k,v| [t(k),k]}), {}, {class: 'form-control'} %>
  </article>

  <article class="form-group">
    <%= submit_tag t(:submit), class: 'btn btn-primary' %>
  </article>

<% end %>